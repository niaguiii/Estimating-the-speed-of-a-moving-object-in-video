# FastAPI Web后端依赖

# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 数据验证
pydantic>=2.0.0

# 文件处理（可选）
aiofiles>=23.0.0

# 注意：
# - CORS支持已包含在FastAPI中，无需额外安装
# - 确保主项目的requirements.txt已安装（YOLOv8等依赖）
