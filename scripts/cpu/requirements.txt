# Phase 3 Dependencies - RAFT Optical Flow + Depth Anything V2
# Use this file for Phase 3 development

# ====================================================================
# Core Deep Learning Framework (Updated for RAFT + Depth)
# ====================================================================
torch>=2.0.0              # PyTorch 2.0+ (required for torchvision RAFT)
torchvision>=0.15.0       # Torchvision with built-in RAFT models

# ====================================================================
# Phase 1 & 2 Dependencies (Keep these)
# ====================================================================
ultralytics>=8.0.0        # YOLOv8 + ByteTrack
opencv-python>=4.5.0      # Computer Vision
numpy>=1.21.0             # Numerical Computing
supervision>=0.11.0       # Object Tracking (optional)

# ====================================================================
# Phase 3: Depth Estimation
# ====================================================================
timm>=0.9.0               # PyTorch Image Models (required by Depth Anything)
huggingface-hub>=0.19.0   # For downloading pretrained models
transformers>=4.30.0      # Transformer models (Depth Anything V2)

# ====================================================================
# Image Processing & Visualization
# ====================================================================
Pillow>=8.0.0             # Image processing
matplotlib>=3.5.0         # Visualization
pandas>=1.3.0             # Data processing
tqdm>=4.60.0              # Progress bar

# ====================================================================
# Optional: For better performance
# ====================================================================
# accelerate>=0.20.0      # Multi-GPU support (for cloud GPU)
# xformers>=0.0.20        # Memory efficient attention (optional)

# ====================================================================
# Installation Instructions (CPU - Local Development):
# ====================================================================
# Method 1 (Recommended): Run the installation script
#    cd scripts/cpu
#    install.bat         # Windows
#    ./install.ps1       # PowerShell
#
# Method 2: Manual installation
#    pip install -r scripts/cpu/requirements.txt
#
# For GPU deployment, use scripts/gpu/ instead
# ====================================================================
